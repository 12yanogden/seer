use super::handler::Handler;
use crate::dto::dto::DTO;

pub struct GetSourcesHandler;

impl<'a> Handler<'a> for GetSourcesHandler {
    fn handle(&mut self, dto: &mut DTO<'a>) {
        let strategy = 
    }
}
